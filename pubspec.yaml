name: farmer_geo_locator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  internet_connection_checker: ^1.0.0+1
  location: ^6.0.2
  shared_preferences: ^2.2.3
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

module:
  androindX: true